export interface IColors {
  primary: string;
  secondary: string;
  danger: string;
  text: string;
  textLight: string;
  textDark: string;
}

export interface ITypeOptions {
  id: number;
  description: string;
  notation?: string;
}
